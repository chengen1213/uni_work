void draw() {
  noLoop();
  int a = round(random(1, 10));
  int b = round(random(1, 10));
  println(add(a, b));
  println(multi(a, b));
}

int add(int a, int b) {
  return a+b;
}
int multi(int a, int b) {
  return a*b;
}