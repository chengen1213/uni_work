void draw() {
  noLoop();
  int [] my_array = {1, 2, 3}; 
  print_cumsum(my_array);
}
void print_cumsum(int [] values) {
  int sum = 0;
  for (int value : values) {
    sum += value;
  }
  println("The cumulative sum for this array is: " + sum);
}